# Project Integrity Audit — repository `ssl-ecas` as of `rev9-frontier` (d4c6efb)

> **SIMULATED REVIEW MATERIAL.** Generated 2026-07-05 as the
> project-as-a-whole component of the ARS review run (pipeline
> "integrity check" stage, adapted). Every claim below names the command or
> file that evidences it. Complements the panel's methodology review
> (`r1_methodology.md`), which independently traces manuscript numbers.

## 1. Number provenance — PASS (headline sample)

Manuscript headline values traced to the measured record:

| Manuscript claim | Source | Match |
|---|---|---|
| ECA survival 0.985 vs replicate benchmark 0.987 [0.978, 0.992] (abstract, Table) | RESULTS.md:121–122 | ✓ |
| Exact rule recovery 100% ECA / 97.5% radius-2 (abstract) | RESULTS.md:232, 266 ("1.00 / 0.975") | ✓ |
| Stacking increment +0.32, base 0.556 → 0.876 (abstract, §IV.C) | RESULTS.md:171 ("+0.320 CI[0.23,0.48], base 0.556 → stacked") | ✓ |
| Mechanistic 283 ms, break-even ≈4,700 queries (§IV.F) | RESULTS.md (single occurrence each, rev-8 C8 section) | ✓ |
| Landscape prevalence 7.8% Wilson [6.9, 8.8]% | RESULTS.md rev-7/8 record | ✓ |
| Split sensitivity: LOO-88 survival 0.73; 20 outer splits 0.76 (0.56–0.84) | main.tex:733–738 ↔ RESULTS.md rev-8 C5 | ✓ |

No mismatch found in the sample. (The methodology reviewer's report carries an
independent, larger trace.)

## 2. Pre-registration discipline — PASS with one caveat

`git log -- EVALUATION_CRITERIA.md` shows 8 revision commits (rev 2→9), each
before its corresponding measurements:

- **rev 9** `8f5cb56` (07-05 01:43) precedes machinery `717c297` (01:58), F1
  grid `111e3df` (10:00), F2+F3 `0f8e691` (14:04). **Verified ordering.**
- **rev 8** `75e1d42` (07-04 20:40) precedes C2–C8 measurement `fa994da`
  (07-05 00:52) and the RESULTS record `28eb9fd` (00:53). **Verified ordering.**
- revs 5–7 (`e2dd185`, `516866b`, `a809b2a`): commit messages state
  numbers-free registration and the log ordering is consistent; not
  commit-by-commit re-verified here.
- **Caveat — criterion 7:** `c2b99cb` (07-03) contains the pre-registration
  *and* the measured result **in the same commit**. The message attests the
  bars (0.5/0.2) were fixed before measurement, and the commit honestly
  records an INCONCLUSIVE first outcome — but for this one criterion the
  registration-before-measurement claim is attested rather than git-auditable.
  Worth a one-line disclosure if criterion 7 ever becomes load-bearing in the
  manuscript (currently it supports the nuCA by-product only).

## 3. Reproducibility affordances — PASS

- **Tests**: `pytest -q` in the `ssl-ecas` conda env: **208 passed**, 0 failed
  (47.6 s; 35 benign sklearn RuntimeWarnings in one salience test).
- **Lint**: `black --check` (83 files unchanged) and `ruff check` both clean.
- **Checkpoint provenance**: the canonical direct-CNN checkpoint
  (`runs/m4_range2_seed0/checkpoint_final.pt`) is documented, and RESULTS.md:93
  records the discarded wrong-checkpoint frontier run with the bit-exact
  reproduction audit — exemplary provenance hygiene.
- **Target determinism**: the former `dynamics_feature_matrix` RNG
  set-dependence is **fixed** (identity-keyed `SeedSequence([seed, rule,
  radius])`, cross-set drift now exactly 0; RESULTS.md:820–828, registered as
  rev-6 S3). The stale "deferred bug" note in internal memory should be
  updated.
- **Environment**: dedicated conda env + `pip install -e ".[dev]"` documented
  in README "Install"; device policy (MPS→CPU, fp32) in CLAUDE.md/README.

## 4. Documentation — PASS

- `DECISIONS.md`: 10 dated executive-decision rows, each citing its source.
- `README.md`: "Manuscript & review history" section present (line 271) with
  the review rounds and prereg revision trail; repository layout and tests
  sections current.
- Notebooks `01–05` present; 01–04 carry project-arc banners, 05 narrates the
  review-response arc (commits `4bd7de4`, `5048ddf`).

## 5. Findings / open items

| # | Item | Severity | Evidence |
|---|---|---|---|
| P1 | **Figure assets contradict the corrected claims**: `make_figures.py:33` still labels the landscape band `complex (...)` (retired for "damage-signature") and `make_figures.py:69` titles the map figure "the learned map has no advantage" (corrected verdict: inconclusive). If `landscape.pdf` was generated by this script, the in-figure legend visible to referees says "complex". | **Should fix before round 3** | grep of `manuscript/make_figures.py`; independently flagged by the EIC review |
| P2 | `map_benchmark.pdf` sits in `manuscript/figures/` but is not included by `main.tex` (orphaned asset) | Minor | `ls figures/` vs `grep figures/ main.tex` |
| P3 | Author-block TODOs outstanding (ORCIDs, corresponding email) | Blocking for submission, user-gated | main.tex:19–21 |
| P4 | Repository private during review → registration claims not auditable by the referee; anonymity rationale inconsistent with the signed byline | Should fix (see `rebuttal_audit.md` R1/R2) | response letter §10 |
| P5 | Criterion-7 prereg+measurement in one commit | Disclosure-worthy, low risk | §2 above |
| P6 | `rev9-frontier` intentionally unmerged so the submitted PDF stays frozen — by design, not a defect | Informational | branch state |

## Verdict

The project's scientific bookkeeping is in excellent shape: clean test/lint
state, verifiable prereg ordering for the load-bearing revisions (8, 9),
headline numbers fully traceable, and unusually candid provenance notes. The
actionable items are P1 (stale figure labels — the only place the repo still
contradicts the corrected manuscript), P3 (author metadata), and P4 (make the
registration auditable to the referee at round 3).

% Manuscript for Chaos: An Interdisciplinary Journal of Nonlinear Science
% (AIP), REVTeX 4.2 with the AIP `cha` journal option.
\documentclass[aip,cha,amsmath,amssymb,reprint,floatfix]{revtex4-2}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue}

\newcommand{\rtwo}{$R^2$}

\begin{document}

\title{Rule reconstruction from a single space--time diagram yields
simulation-limited prediction of the finite-horizon damage response of cellular
automata}

% TODO: add ORCIDs + corresponding email before submission.
\author{Michiel Rollier}
\email{michiel.rollier@ugent.be} % TODO: confirm corresponding address
\affiliation{Department of Data Analysis and Mathematical Modelling,
Ghent University, Ghent, Belgium}
\author{Jan M. Baetens}
\affiliation{Department of Data Analysis and Mathematical Modelling,
Ghent University, Ghent, Belgium}

\date{\today}

\begin{abstract}
Damage-response statistics of a cellular automaton (CA), whether and how far
the difference between two runs differing in one cell spreads, are defined
through \emph{twin} simulations, so estimating them from one observed
diagram is a natural task for learned amortized models. We benchmark that
task under pre-committed decision rules, over the $88$ elementary-CA orbits
and $800$ sampled radius-two rules, for complete, noiseless, binary diagrams
of known radius. Under these matched assumptions a single $127$-cell diagram
identifies the rule table for $100\%$ (elementary) and $97.5\%$ (radius-two)
of held-out rules, and re-running the perturbation experiment on the
reconstructed rule predicts all four statistics at the accuracy of an
independent Monte-Carlo replicate of the target. We distinguish this
simulation-limited benchmark ($2\,\mathrm{ICC}-1$) from the latent-reliability
ceiling (ICC). Two direct learned estimators fall far short of the benchmark
on damage survival, though the network carries survival information the cheap
statistics miss (incremental \rtwo{} $+0.32$). An identifiability frontier
maps where rule reading fails: an approximate Bayesian rule decoder extends
it to $50\%$ masking, extreme initial densities, and ${\sim}3\%$ noise; the
noise band beyond belongs to training for the corruption, where a
degradation-augmented direct network is strongest, an ordering that
replicates across rule panels and training seeds even though its level does
not. Wherever the rule is recoverable, no tested direct estimator matches
reading it, not even an unconstrained network $354\times$ larger. When the
observation encodes the generator, system identification, whose accuracy is
set by simulation budget rather than training, is the baseline to beat.
\end{abstract}

\maketitle

\begin{quotation}
Machine learning is increasingly used to read dynamical properties---chaos,
stability, sensitivity to perturbation---directly off a single picture of a
system's evolution, skipping the perturbation experiments that define those
properties. Cellular automata are the cleanest laboratory for asking what
such shortcuts really measure, because the picture often contains enough
information to rebuild the generating rule itself. We make that alternative
explicit: rebuild the rule from one diagram, re-run the perturbation
experiment, and compare. Under a matched observation model this transparent
route is as accurate as re-measuring the ground truth, while two learned
models fall well short of it. Degrading the observation reveals an orderly
frontier where exact reconstruction fails, probabilistic variants extend it,
and beyond them the race is won by whichever estimator was trained for the
corruption at hand. The result is a calibrated, pre-registered template for
fair baselines in data-driven analysis of dynamical systems, and a map of
when each strategy is the right one.
\end{quotation}

\section{Introduction}
\label{sec:intro}

Cellular automata (CA) are the canonical minimal model of emergent computation:
a single local update rule, iterated in parallel, produces the full zoo of
behaviours from frozen order to chaos to the localized interacting ``particles''
of complex rules~\cite{wolfram1984,wolfram2002,langton1990,boccara1991}. Classifying that behaviour is a
long-standing and subtle problem, because the field distinguishes the
\emph{genotype}---the local update rule---from the \emph{phenotype}---the
mesoscopic structure of the space--time diagram it generates. A method intended
to read behaviour off the diagram must be prevented from instead reading the
\emph{rule} off it: whenever the diagram exercises the table under the
assumed deterministic, synchronous update it contains, in its local
neighbourhoods, the entire rule table---CA and convolutional networks are
formally close~\cite{gilpin2019}---and a convolutional classifier recovers
the elementary-rule label from a single clean diagram with $99.9\%$ test
accuracy unless architecturally impeded~\cite{rollier2024cnn}. This is an instance of
\emph{shortcut learning}~\cite{geirhos2020shortcut} in its cleanest form.

One natural formalization of Wolfram-style class membership is
undecidable~\cite{culikyu1988}---the obstruction applies to that asymptotic
formalization, not to every finite protocol---and the class observed in
practice depends on the initial-condition measure~\cite{gilman1987}, the
lattice, and the horizon. The defensible object
of study is therefore not ``the class of a rule'' but a \emph{protocol
tuple}---(rule orbit, IC measure, lattice, horizon)---characterized by
finite-horizon, perturbation-defined statistics. The best-posed of these is
\emph{damage spreading}: evolve two copies of a configuration that differ in a
single cell and measure how the difference grows. The construction has a long
lineage---Kauffman's damage experiments on Boolean
networks~\cite{kauffman1969}, the annealed treatment of Derrida and
Pomeau~\cite{derridapomeau1986}, and its formalization for CA, where the
damage growth rate defines a maximal Lyapunov
exponent~\cite{bagnoli1992,baetens2010}. Our targets are the finite-horizon,
protocol-indexed versions of exactly these
quantities~\cite{vispoel2024damage,vispoel2022progress}: we call them
\emph{damage-response statistics}, not invariants, because they are
finite-size, finite-horizon and measure-dependent by construction.

Because damage-response statistics require \emph{two} runs of the simulator,
they are not a function of any single observed diagram, which makes them a
natural target for \emph{amortization} in the sense of simulation-based
inference---pay for training once, then answer per observation at almost no
cost~\cite{cranmer2020}. Benchmarks of this shape, in which a learned model
predicts a dynamical property directly from an observation of a
deterministic system, are increasingly common, and comparisons against
strong classical baselines are documented to be frequently absent or weak in
that literature~\cite{mcgreivy2024}. Under the
observation model in which such benchmarks are usually posed---the diagram is
complete, noiseless, binary, generated by a deterministic synchronous CA of
known radius---the strong baseline is a transparent alternative with
thirty-five years of prior art: \emph{infer the rule from the diagram and
simulate the statistic}. Identifying a binary CA from spacetime data by
tabulating observed transitions is an established inverse problem, essentially
solved under matched assumptions: posed for experimental data by Richards,
Meyer and Packard~\cite{richards1990}, treated at monograph length by
Adamatzky~\cite{adamatzky1994}, extended to noisy observations and unknown
neighbourhoods by Yang and Billings~\cite{yangbillings2000} and to fast
neighbourhood selection by Sun, Rosin and
Martin~\cite{sunrosinmartin2011}, and still active in modern variants---from
nonconsecutive snapshots by constraint projection~\cite{elser2021} to
gradient-trained rules~\cite{mordvintsev2020}
(Appendix~\ref{app:priorwork} tabulates each line's assumptions). We claim no
novelty for the inverter; what has not been measured is what this
thirty-five-year-old fact does to the amortization benchmark. One structural
consequence deserves stating up front. Because the targets are
simulation-defined, successful identification reproduces the target's own
measurement procedure: run at the target's Monte-Carlo budget it attains the
independent-replicate benchmark \emph{by construction}, and run at a larger
budget it climbs to the latent ceiling, so identification yields a
\emph{budget-indexed family} of estimators, not a point
(Sec.~\ref{sec:reliability} quantifies the $1-\mathrm{ICC}$ gap between the
two benchmarks---the entire noisy-score headroom any direct estimator holds
over the family's matched-budget member). Wherever the rule is exactly
recoverable, direct amortization can therefore win only on cost, with
simulation budget now part of the cost, or on robustness to observation
degradation. What remains empirical is how close identification comes to its
benchmark at matched budget, whether the learned estimators recover the
latent mean or merely fit the noisy cache, and where the matched regime
ends. Three results organize everything that follows.

\emph{First}, single-diagram identification, stipulated as prior art, is
confirmed at scale under our protocol ($100\%$ of elementary and $97.5\%$ of
sampled radius-two held-out rules recovered exactly), and
reconstruct-and-simulate then predicts the damage response at the accuracy
of an independent Monte-Carlo replicate of the target. The contribution is
not the inverter but the calibration: a \emph{simulation-limited} benchmark
that knows the difference between ``as good as re-measuring the target'' and
``as good as the truth'' (Sec.~\ref{sec:reliability}). \emph{Second}, the
two direct estimators we trained---a five-statistic regressor and a
convolutional amortiser built to resist first-layer rule reading---fall well
short of that benchmark exactly where texture is least informative (damage
survival), and neither dominates the other; the network does carry survival
information the five statistics miss, a real and quantified complementarity.
\emph{Third}, the mechanistic route is brittle outside its assumptions:
exact reconstruction collapses under modest noise, heavy masking, or extreme
initial-condition density (Sec.~\ref{sec:identifiability}), and the
estimator race across that frontier (Sec.~\ref{sec:frontier}) shows what
replaces it---an approximate Bayesian rule decoder in the intermediate
masking and density band, and beyond ${\sim}3\%$ noise whichever estimator
is trained for the corruption, with measured seed sensitivity and an
independent-panel replication reported alongside. Wherever the rule is
recoverable, no tested direct estimator beats reading it, not even an
unconstrained network $354\times$ larger. The frontier, not the
matched-regime verdict, is what transfers: it maps which estimator earns its
keep in each observation regime.

We committed decision rules, margins, and rule panels to a timestamped
repository before the corresponding measurements; Appendix~\ref{app:repro}
labels each analysis as \emph{registered}, \emph{post-hoc confirmatory}, or
\emph{exploratory}, and the repository is released with the article.

\section{Benchmark}
\label{sec:methods}

\subsection{Rule spaces and the observation model}
We use two rule spaces. \emph{Elementary CA} (ECA): two states, radius one,
$2^{2^3}=256$ rules, of which $88$ are distinct representatives under the
reflection and complementation symmetries a behavioural method should
respect~\cite{lipackard1990}.
\emph{Radius-two CA}: two states, a five-cell neighbourhood, a $32$-entry table
and $2^{32}\approx4\times10^{9}$ rules; ECA embeds exactly (a continuity
control). The radius-two space is sampled uniformly ($800$ canonical rules after
orbit deduplication).

Every quantity is indexed by a fixed \emph{observation protocol}: a
Bernoulli$(1/2)$ initial condition on a ring of $127$ cells (non-power-of-two,
so additive rules do not collapse to homogeneity), evolved from $t=0$; the
damage horizon is $\lfloor w/(2r)\rfloor-1$ steps so a perturbation cone cannot
wrap. The observation model deliberately matches the generator: diagrams are
\emph{complete, noiseless, binary, synchronous in the observed time direction,
spatially uniform in the rule, and of known radius}. These assumptions are load
bearing---they are what make the rule readable---and
Sec.~\ref{sec:identifiability} measures what happens when each is relaxed.

\subsection{Damage-response statistics}
The targets are four scalars per rule, estimated from twin runs: draw an
initial condition, flip one uniformly chosen cell, evolve both copies to the
horizon, and compare final rows. \emph{Damage survival} is the probability that
any difference remains; conditional on survival, \emph{damage fraction} is the
fraction of the ring that differs, \emph{spreading rate} is the damage-cone
extent normalised by $2r$ times the number of steps (so $1$ is light speed at
any radius), and \emph{cone fill} is the density of differing cells within the
cone (exact formulas in Appendix~\ref{app:methods}). Survival is a
finite-horizon \emph{proxy} for the sign of the CA's damage-based maximal
Lyapunov exponent~\cite{bagnoli1992,baetens2010}---the asymptotic exponent
itself is not defined by any finite protocol---and the spreading rate is
likewise the finite-horizon damage-front velocity of that construction. Each target vector is a
Monte-Carlo average over $n_{\mathrm{pairs}}=256$ initial conditions from a
random stream keyed on the rule's identity, so a rule's target is a
well-defined function of (rule, protocol), reproducible under a fixed seed but
Monte-Carlo limited (Appendix~\ref{app:repro}).

\subsection{Three reliability benchmarks, distinguished}
\label{sec:reliability}
Because each cached target is a single Monte-Carlo estimate, no predictor
can agree with it perfectly, and ``the ceiling'' depends on \emph{what kind
of predictor} is being scored: conflating the cases misstates a method by
several times the margins at stake. From $K=20$ independent target
replicates per rule we report, per target, with rule-bootstrap confidence
intervals [registered, rev.~8]:

\begin{enumerate}
\item \emph{Latent-target reliability} $\mathrm{ICC}(1)=\sigma_a^2 /
(\sigma_a^2+\sigma_e^2)$ from a one-way random-effects decomposition
($\sigma_a^2$ between-rule signal, $\sigma_e^2$ Monte-Carlo noise): the
attenuation ceiling for a predictor of the \emph{noise-free} target mean.
\item \emph{Independent-replicate agreement}: the empirical \rtwo{} of one
replicate predicting another. Under the idealization
$Y_1=\theta+\varepsilon_1$, $Y_2=\theta+\varepsilon_2$ with equal,
independent, additive noise this is
$1-2\sigma_e^2/(\sigma_a^2+\sigma_e^2)=2\,\mathrm{ICC}-1$, \emph{not} ICC.
This is the appropriate benchmark for a correctly identified rule followed by
an independent simulation at the same $n_{\mathrm{pairs}}$---an estimator
that returns a fresh Monte-Carlo draw, not the latent mean.
\item \emph{Large-simulation reference}: the same estimator scored against
near-noise-free targets recomputed at $n_{\mathrm{pairs}}=4096$; as the
comparison target's noise vanishes, an optimal fresh-simulation estimator
should rise from benchmark (2) to benchmark (1).
\end{enumerate}

The gap between the two ceilings, $\mathrm{ICC}-(2\,\mathrm{ICC}-1) =
1-\mathrm{ICC}$, is the entire noisy-score headroom available to \emph{any}
estimator over a matched-budget fresh simulation; identification claims it by
raising its own $n_{\mathrm{pairs}}$, a direct model only by actually
estimating $\theta$. Because survival and cone fill are markedly
heteroscedastic across rules, we do not lean on the Gaussian identity:
benchmark (2) is \emph{measured} as the mean pairwise \rtwo{} over the $K=20$
replicates (the identity is reported as a cross-check), ICC(1) comes from the
one-way variance decomposition used descriptively, and every interval is a
rule-level bootstrap rather than a Gaussian standard error. The measured
values are the two rightmost columns of Tables~\ref{tab:eca}
and~\ref{tab:r2}; survival is the noisiest target (ECA Monte-Carlo std
$0.022$). Every \rtwo{} in this paper is read against the benchmark
appropriate to the estimator's type, and the ``simulation-limited'' claim
itself is tested by a \emph{paired} rule-bootstrap equivalence analysis with
a pre-registered margin (Sec.~\ref{sec:results-mech}).

\section{Estimators}
\label{sec:estimators}

\emph{(i) Matched-model system identification (``mechanistic'').} From one
diagram we tabulate every observed neighbourhood$\to$next-cell transition
over all consecutive row pairs (the $t{=}0$ row included as an input row,
periodic in space) and rebuild the $2^{2r+1}$-bit rule table, then re-run the
twin-run protocol on the reconstructed rule with an independent random
stream. Conflicting observations of the same neighbourhood---impossible for
a clean deterministic CA, routine under the degradations of
Sec.~\ref{sec:identifiability}---are resolved by \emph{majority vote} (ties
to $0$), with an inconsistency flag; pseudocode for this and every degraded
decoder is in Appendix~\ref{app:algorithms}. The estimator assumes exactly the
observation model above; it is \emph{not} assumption-free, and it carries
one explicit prior: table entries never exercised by the diagram must be
completed. The registered default completes
them with $0$; we report the observed \emph{coverage}, compare default-$0$,
default-$1$ and empirical-prior completions, and provide a posterior-averaged
variant that enumerates or samples the $2^k$ completions of $k$ missing entries
and propagates the spread into a predictive interval [registered, rev.~8].
Coverage proves a non-issue under this protocol at either evaluation unit
(Sec.~\ref{sec:results-mech}).

\emph{(ii) Five cheap statistics.} A regressor on five label-free
single-diagram features (polarity-folded density, temporal activity, zlib
compression ratio~\cite{zenil2010}, $2\times2$ block entropy, and a
radius-aware input-entropy
variance~\cite{wuensche1999}), all exactly or approximately invariant under the
orbit symmetries. Both a ridge and a gradient-boosted regressor are fit; the
stronger (the boosted one) is the baseline of record
(Appendix~\ref{app:methods}).

\emph{(iii) Direct convolutional amortiser.} A small CNN ($31{,}524$
parameters; full layer table in Appendix~\ref{app:methods}) whose $2\times2$
first-layer kernels cannot span a neighbourhood together with its output cell,
feeding a $64$-dimensional bottleneck and a linear head; BatchNorm keeps the
per-patch \texttt{predict\_map} variant local. Trained $60$ epochs at
$127\,$px; five seeds vary initialization and minibatch order. \emph{All neural
claims in this paper are about this architecture under this training budget},
not about deep networks as a class: we did not run an architecture search, and
Sec.~\ref{sec:discussion} names the learned system-identification pipelines
that remain untested. The ``anti-shortcut'' first layer is an architectural
intent only---Sec.~\ref{sec:probes} shows the rule remains decodable from the
trained bottleneck. Two registered controls (rev.~10) bound what the
constraint and the clean training cost: an unconstrained \texttt{resnet18}
at the same budget, and the same CNN retrained with the rule-reader's
degradation augmentation (Sec.~\ref{sec:frontier}).

\emph{Statistical protocol.} Results are reported per target (never only as a
four-target median), with the held-out \emph{rule} as the unit of resampling.
Paired differences carry rule-level cluster-bootstrap intervals ($10^4$
resamples); each method pair is classified per target as \emph{superiority}
(median $\Delta R^2\ge0.10$, $95\%$ CI excluding $0$), \emph{practical
equivalence} (TOST: $90\%$ CI within $(-\delta,+\delta)$, $\delta=0.05$,
reported also at $\delta=0.02$ and $0.10$), or \emph{inconclusive}---the three
labels are not interchangeable and we use only the one earned. Split
uncertainty beyond the registered split is quantified for the deterministic
methods by leave-one-orbit-out over all $88$ ECA orbits and $20$ repeated outer
splits on radius two; the CNN is reported at the registered split with its
five-seed spread, and retraining across splits was not performed. A
cross-fitted stacking test measures incremental \rtwo{} (margin
$0.02$, CI excluding $0$) in both directions between the network and the five
statistics.

\section{Results}
\label{sec:results}

\subsection{Rule reconstruction is simulation-limited}
\label{sec:results-mech}

Table~\ref{tab:headline} gives the headline medians; Tables~\ref{tab:eca}
and~\ref{tab:r2} give the per-target values and intervals. The mechanistic estimator recovers the rule exactly for $100\%$ of
ECA and $97.5\%$ of radius-two held-out rules and predicts each damage-response
statistic \emph{at its independent-replicate agreement benchmark}: on ECA,
survival $0.985$ against a benchmark of $0.987$ (CI $[0.978,0.992]$); on radius
two, survival $0.977$ against $0.974$ $[0.945,0.984]$ and cone fill $0.985$
against $0.959$ $[0.900,0.987]$. Because interval \emph{inclusion} of a
benchmark is not an equivalence test, we also ran a registered \emph{paired}
analysis [rev.~11]: the same rule-bootstrap resamples are pushed through both
quantities, giving a CI for the difference
$R^2_{\mathrm{mech}}-R^2_{\mathrm{replicate}}$ against a pre-registered
margin of $\max(0.01,\,1-\mathrm{ICC}_t)$. Six of the eight target--panel
comparisons are formally \emph{equivalent}: on radius two, survival differs
by $+0.001$ (CI $[-0.008,+0.009]$, margin $\pm0.013$) with fraction and rate
tighter still; on ECA, fraction, rate and cone fill pass. The two remaining
comparisons are consistent with equivalence but resolution-limited: ECA
survival's CI ($\pm0.015$ over $18$ rules) exceeds its $\pm0.010$ margin at a
point difference of $+0.0003$, and radius-two cone fill ($+0.006$, CI
$[-0.009,+0.022]$, margin $\pm0.020$) overshoots only on the side where the
mechanistic estimator \emph{beats} the replicate. Under the matched
observation model, reading the rule and re-running the experiment is
equivalent to re-measuring the ground truth wherever the panels can resolve
the question, and never distinguishable in the unfavourable direction. Scored
against the $16\times$-larger reference simulation instead of the noisy cache,
the same predictions rise to the latent ICC ceiling (survival: ECA $0.994$
against ICC $0.994$; radius two $0.988$ against $0.987$)---the residual was
the comparison target's noise, not estimator error [registered, rev.~8]. The
direct estimators do not share that rise: rescored against the same reference
[registered, rev.~11], the CNN's radius-two median moves by $-0.000$ (from
$0.859$) and the boosted baseline's by $+0.007$ (from $0.845$)---so their
shortfall is genuine estimator error; the $1-\mathrm{ICC}$ headroom of a
perfect latent-mean predictor (Sec.~\ref{sec:reliability}) is nowhere in
play for the models we trained.

The ``global'' radius-two panel is deliberately \emph{enriched}: all $57$
\emph{signature-complex} rules---a criterion label meaning the rule meets
the registered damage-signature thresholds of Fig.~\ref{fig:landscape}, not
a validated behavioural class---are force-held (never trained on), so they
form $35.6\%$ of the $160$-rule panel against $7.1\%$ of the $800$-rule
sampling universe. We disclose that signature membership was computed by the
registered criterion-9 thresholds from the same seed-0 target cache that
supplies the evaluation labels. Decomposing the panel [registered, rev.~11]
shows the enrichment is \emph{conservative} for the headline claim: on the
$103$ stratified-random rules alone the mechanistic median is $0.995$
(signature subpanel $0.982$), and post-stratifying the pooled estimate to the
universe prevalence gives $0.994$---while the seed-0 CNN moves the other way,
from $0.859$ enriched to $0.821$ random and $0.830$ post-stratified.
Reweighting to the representative distribution \emph{widens} the gap between
the families.

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{lccccc}
task & mechanistic & 5 statistics & deep CNN & replicate agreement & ICC ceiling \\
\colrule
ECA global & \textbf{0.999} & 0.927 & $0.852\pm0.015$ & 0.998 & 0.999 \\
radius-2 enriched panel & \textbf{0.991} & 0.845 & $0.864\pm0.014$ & 0.985 & 0.993 \\
radius-2 random subpanel & \textbf{0.995} & 0.849 & $0.821^{\ast}$ & 0.985 & 0.993 \\
radius-2 post-stratified & \textbf{0.994} & 0.849 & $0.830^{\ast}$ & 0.985 & 0.993 \\
radius-2 complex subpanel & \textbf{0.982} & 0.795 & $0.832\pm0.022$ & --- & $\approx0.98$ \\
\end{tabular}
\end{ruledtabular}
\caption{Median (over the four targets) leave-rules-out \rtwo{}. The
mechanistic estimator attains the replicate-agreement benchmark (and,
against a near-noise-free reference, the ICC ceiling); both direct
estimators sit well below both. The radius-two panel is \emph{enriched} (all
$57$ signature-complex rules force-held: $35.6\%$ of the panel vs $7.1\%$ of
the sampling universe); the random-subpanel and post-stratified rows
[registered, rev.~11] give the representative view. CNN: mean $\pm$ s.d.\
over five training seeds at the fixed registered split;
$^{\ast}$seed-0 checkpoint, the panel-decomposition artifact of record.
Reliability columns: $250$-rule universe sample, applied panel-wide (none
drawn for the complex subpanel). Per-target values: Tables~\ref{tab:eca}
and~\ref{tab:r2}.}
\label{tab:headline}
\end{table*}

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{lccccc}
ECA target & mechanistic & 5 statistics & deep CNN & replicate agreement & ICC \\
\colrule
damage survival & 0.985 [0.893, 0.997] & 0.708 [$-0.537$, 0.924] & $0.700\pm0.018$ & 0.987 & 0.994 \\
damage fraction & 1.000 [0.997, 1.000] & 0.924 [$-0.840$, 0.959] & $0.789\pm0.038$ & 0.998 & 0.999 \\
spreading rate & 1.000 [0.998, 1.000] & 0.965 [$-0.517$, 0.998] & $0.956\pm0.015$ & 0.999 & 0.9995 \\
cone fill & 0.999 [0.991, 1.000] & 0.929 [0.849, 0.994] & $0.916\pm0.019$ & 0.998 & 0.999 \\
\end{tabular}
\end{ruledtabular}
\caption{Per-target held-out \rtwo{} on the elementary space ($18$ held-out
orbits). Brackets: rule-bootstrap $95\%$ CIs (test-rule sampling, conditional
on the trained model); CNN: mean $\pm$ s.d.\ over five seeds (training
stochasticity at the fixed registered split). The five-statistic column's
split sensitivity (leave-one-orbit-out over
all $88$ orbits) is reported in Appendix~\ref{app:methods}. The shortfall of
both direct estimators concentrates on damage survival, the least texture-like
and least reliable target.}
\label{tab:eca}
\end{table*}

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{lccccc}
radius-2 target & mechanistic & 5 statistics & deep CNN & replicate agreement & ICC \\
\colrule
damage survival & 0.977 [0.930, 0.989] & 0.809 [0.554, 0.906] & $0.874\pm0.009$ & 0.974 & 0.987 \\
damage fraction & 0.997 [0.995, 0.998] & 0.909 [0.878, 0.932] & $0.880\pm0.020$ & 0.997 & 0.998 \\
spreading rate & 0.997 [0.996, 0.998] & 0.880 [0.839, 0.912] & $0.865\pm0.020$ & 0.997 & 0.998 \\
cone fill & 0.985 [0.969, 0.992] & 0.596 [0.320, 0.775] & $0.337\pm0.109$ & 0.959 & 0.980 \\
\end{tabular}
\end{ruledtabular}
\caption{Per-target held-out \rtwo{} on the radius-two space ($160$ held-out
rules; an \emph{enriched} panel: all $57$ signature-complex rules
force-held, representative decomposition in Table~\ref{tab:headline}).
Brackets and CNN uncertainty as in Table~\ref{tab:eca}; the baseline's
spread over $20$ repeated outer splits is in Appendix~\ref{app:methods}.}
\label{tab:r2}
\end{table*}

The completion prior is exposed rather than hidden. Per \emph{rule},
coverage is complete for every ECA held-out table and for $156/160$
radius-two tables; the four exceptions miss one entry each, the
default-$0$/default-$1$/empirical completions change the radius-two median
\rtwo{} only in the fourth decimal ($0.9905$--$0.9908$), and the
posterior-averaged variant supplies predictive intervals precisely for those
rules. Per \emph{diagram} [audit registered, rev.~11], over all $10{,}240$
radius-two held-out diagrams the exact-reconstruction rate is $0.990$ with
median coverage $1.0$; over all $4{,}608$ ECA diagrams both are $1.0$. The
estimator carries a weak, testable prior---it is not assumption-free.

\subsection{No stable ordering between the direct estimators}
\label{sec:paired}

The paired comparisons [registered, rev.~7--8] give the network and the
five-statistic baseline no global ranking. On radius two ($160$ held-out rules,
adequately powered) the CNN is ahead on damage survival ($\Delta R^2=+0.08$,
$95\%$ CI $[+0.002,+0.201]$ excluding $0$ but below the $0.10$ superiority
margin), the baseline is \emph{superior} on cone fill ($\Delta R^2=-0.21$, CI
$[-0.38,-0.07]$), and fraction and rate are \emph{inconclusive} at
$\delta=0.05$ (equivalent only at the loose $\delta=0.10$; fraction's CI
$[-0.09,-0.00]$ weakly favours the baseline). On ECA ($18$ held-out orbits)
every CNN--baseline contrast is inconclusive except spreading rate (equivalent
at $\delta=0.05$); that panel is underpowered for tight per-target intervals
and we do not read a ranking into it. The mechanistic estimator,
by contrast, meets the registered superiority rule against the baseline on
survival, rate and cone fill ($\Delta R^2$ up to $+0.39$) and against the CNN
on fraction, rate and cone fill (up to $+0.60$); the two remaining contrasts
(fraction vs.\ the baseline, survival vs.\ the CNN) have intervals excluding
zero in its favour with point differences just below the margin. No comparison
on any target in either space favours a direct method over it.

\subsection{What the network adds: complementarity, not redundancy}
\label{sec:stacking}

The stacking test [registered, rev.~7] asks whether one method's prediction
explains held-out residual variance beyond another's. It is a
\emph{cross-fitted diagnostic computed within the held-out panel}, licensing
a complementarity conclusion and not a deployment one: each row is one
held-out rule, the base is a repeated-$K$-fold out-of-fold ridge on the five
statistics with standardization refit inside every fold, and the CNN
predictions come from the already-trained network without any use of
held-out labels. The base is therefore \emph{not} the boosted baseline of
record (survival $0.56$ versus $0.81$, cone fill $0.14$ versus $0.60$). The
answer is target-specific: adding the CNN to the five statistics yields a
large, significant increment on damage survival ($+0.32$, CI
$[+0.23,+0.48]$, base $0.56\to0.88$), small significant increments on
fraction and rate, and none on cone fill; the reverse direction adds
nothing. With the boosted baseline of record as base [post-hoc confirmatory,
rev.~10] the survival increment remains significant but smaller ($+0.08$, CI
$[+0.02,+0.18]$, $0.80\to0.88$): the $+0.32$ headline is base-specific; the
base-independent statement is that the network carries survival information
both bases miss. Whether that information is \emph{harvestable} has a
measured answer [registered, rev.~11]: a deployment-style stack (the same
regressors, ridge fit only on training rules, applied once to the untouched
panel) reaches an overall median of $0.88$ but realizes essentially none of
the survival increment ($+0.001$ over the CNN alone, n.s.; fraction $+0.02$
and rate $+0.03$ do transfer). Only the within-panel diagnostic detects the
survival signal: the network \emph{carries} information that texture
statistics miss---exactly where both direct methods sit farthest below the
reliability benchmark---but a practitioner cannot yet buy it with a linear
meta-model. The mechanistic estimator's increment over the five statistics
remains far larger on every target ($+0.14$ to $+0.84$), so the practical
order of preference under the matched observation model is clear.

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{lccc}
target & CNN over 5 stats & 5 stats over CNN & mechanistic over 5 stats \\
\colrule
damage survival & $+0.32$ [0.23, 0.48] (0.56 $\to$ 0.88) & $-0.01$ [$-0.03$, $-0.00$] & $+0.42$ [0.33, 0.71] (0.56 $\to$ 0.97) \\
damage fraction & $+0.03$ [0.01, 0.06] (0.85 $\to$ 0.89) & $+0.01$ n.s. & $+0.14$ [0.11, 0.18] (0.85 $\to$ 0.99) \\
spreading rate & $+0.03$ [0.01, 0.06] (0.84 $\to$ 0.87) & $+0.01$ n.s. & $+0.16$ [0.12, 0.20] (0.84 $\to$ 0.99) \\
cone fill & $+0.08$ n.s. & $+0.08$ n.s. & $+0.84$ [0.66, 1.01] (0.14 $\to$ 0.98) \\
\end{tabular}
\end{ruledtabular}
\caption{\emph{Cross-fitted complementarity diagnostic, computed within the
held-out panel} (not a train-once stacked estimator; see text and the
deployment-style result). Incremental \rtwo{} with rule-bootstrap $95\%$ CIs
(radius-two held-out rules; rows are rules, folds grouped by rule, all
transformations fit inside folds), and the full stacked-model \rtwo{} in
parentheses (base $\to$ stacked). ``n.s.'': CI includes $0$ or increment below
the registered $0.02$ margin. Percentile intervals of an increment may exceed
$1$ when a bootstrap draw's base \rtwo{} is negative.}
\label{tab:stacking}
\end{table*}

\subsection{Why: the rule is readable, including from the network}
\label{sec:probes}

The mechanism behind these results is that the local rule pervades every
representation of the diagram. The deterministic inverter recovers it exactly
(above); linear probes quantify the same fact for the learned and handcrafted
representations, in two tasks that answer different questions. \emph{Identity probes} (stratified split over diagrams;
train and test diagrams disjoint, rule classes shared---within-rule decoding
across new diagrams; $88$ and $800$ classes, majority baseline $\approx0.011$
and $0.001$) recover the rule from the CNN's own $64$-d bottleneck at balanced
accuracy $0.88$ (ECA) and $0.95$ (radius two), versus $0.65$/$0.30$ from the
five statistics. \emph{Truth-table-bit probes} (leave-rules-out GroupKFold;
test rules never seen by the probe---out-of-rule transfer of the reading
mechanism; chance $0.5$) reach $0.78$/$0.61$ from the bottleneck versus
$0.69$/$0.55$ from the statistics. The $2\times2$ first layer therefore does
\emph{not} prevent rule recovery; we retired that claim. What the probes
establish is accessibility, not use: they show the information survives into
the bottleneck, and the mechanistic estimator shows a rule-first route attains
the reliability benchmark. Whether the network's regression head \emph{causally
relies} on rule information is not established here; rule-scrubbing and
rule-provision interventions that would test it are future work
(Sec.~\ref{sec:discussion}).

\subsection{When reading fails: the identifiability phase diagram}
\label{sec:identifiability}

The mechanistic route is only as good as its observation model, so we mapped
its failure surface [exploratory, rev.~8]. Figure~\ref{fig:ident} sweeps, on
a fixed-seed $80$-rule subsample of the $160$ radius-two held-out rules (a
compute constraint; the same panel is used throughout
Secs.~\ref{sec:identifiability}--\ref{sec:frontier}), four observation axes;
per setting it reports
table coverage, exact-reconstruction rate, and the median held-out \rtwo{} of
the mechanistic estimator, the five statistics, and the \emph{frozen} CNN (no
retraining; axes that change the input geometry omit the CNN, as noted).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/identifiability.pdf}
\caption{Identifiability of the local rule at radius two ($80$ held-out
rules; median held-out \rtwo{}; sweep simulations at $n_{\mathrm{pairs}}=64$
per cell versus $256$ for the production targets). (a) Observed rows: a
$127$-wide diagram identifies the table from as few as $4$ rows (the CNN,
trained at fixed geometry, is omitted on this axis). (b) Bit-flip noise:
exact reconstruction (dotted) collapses immediately; the mechanistic
estimate (green) falls below the frozen CNN (blue) beyond ${\sim}2$--$5\%$.
(c) Masking: inference tolerates ${\sim}25\%$ missing cells, then collapses.
(d) Initial-condition density: the mechanistic estimator re-simulates under
the \emph{reference} protocol and holds its benchmark across the mid-range
but fails at extremes, where the diagram no longer exercises the table; the
frozen CNN and the statistics degrade off their training density (vertical
dotted line, $0.5$).}
\label{fig:ident}
\end{figure}

Three regimes emerge. Where the observation model is matched (clean, full,
mid-density), reconstruction is exact after a handful of rows and the
mechanistic estimator holds its benchmark---a regime in which single-diagram
amortization of this task family has no room to add value. Under degradation
the picture inverts: at $5\%$ bit-flip noise the mechanistic median \rtwo{}
falls below zero while the frozen CNN retains $0.09$--$0.43$; under masking the
crossover sits near $40\%$; at density $0.1$ all methods fail but none
catastrophically like the inverter under noise. The boundary is sharp because a
deterministic table has no noise model---one corrupted transition flips an
entry.

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/identifiability_v2.pdf}
\caption{The estimator race across the identifiability frontier (radius two,
canonical $80$-rule panel; median held-out \rtwo{}; curves leaving the
bottom of a panel are negative; the noise axis is replicated on the
complementary $80$ rules in Sec.~\ref{sec:frontier}). Solid/dashed pairs:
pseudo-posterior decoder with $\varepsilon$ supplied/self-estimated (red
triangles); learned reader, MAP/sampled (purple diamonds). Noise (top left):
every estimator except the sampled reader collapses by $5$--$7.5\%$ on this
panel. Masking (top right): the pseudo-posterior extends the deterministic
inverter's tolerance from ${\sim}25\%$ to $50\%$. Extreme initial-condition
density (bottom left): the posterior rescues under-exercised tables.
Label-polarity flips (bottom right): the read-then-simulate family is
exactly invariant; the direct CNN and the stack degrade. Grid simulations at
$n_{\mathrm{pairs}}=48$, $8$ posterior-table samples per diagram (production
targets: $256$ pairs); the rule-reader is a single training seed. Registered
numbers-free as rev.~9; the posterior on these axes is post-hoc
confirmatory, the reader and the label axis exploratory.}
\label{fig:frontier}
\end{figure*}

\subsection{Estimators for the degraded regime: the frontier race}
\label{sec:frontier}

The natural repairs are a rule-\emph{posterior} simulator in the
intermediate regime and a learned rule-reader beyond it, and we benchmark
both [registered numbers-free as rev.~9 before measurement; per-estimator
status in Fig.~\ref{fig:frontier}].

\emph{(i) Approximate Bayesian rule decoder}---the probabilistic move of the
identification programme~\cite{adamatzky1994,yangbillings2000}. The model is
an \emph{entrywise pseudo-posterior}: each observed transition count enters
an independent per-entry Binomial likelihood in which a bit flip corrupts
only the \emph{output} cell at rate $\varepsilon$ (supplied, or estimated
from observation self-consistency over a fixed ten-point grid), under an
independent Bernoulli prior per entry at the observed marginal
one-frequency. Input-side corruption---which mis-bins transitions and
correlates overlapping neighbourhoods---is deliberately \emph{not} modelled;
it is absorbed into the effective $\varepsilon$, and the calibration
measurement below prices that approximation empirically. Algorithms and a worked example are in
Appendix~\ref{app:algorithms}; ``posterior'' hereafter abbreviates this
pseudo-posterior. Simulating tables sampled from it \emph{restores
identification across the intermediate band}: at $40\%$ masking
its median \rtwo{} is $0.85$ against the deterministic inverter's $0.24$; at
$50\%$, $0.59$ against $-0.77$; at IC density $0.1$, $0.77$ against $0.08$;
and it is exactly polarity-invariant where the frozen CNN loses accuracy under
label flips ($0.99$ vs $0.59$). Self-consistency estimation of $\varepsilon$
matches the supplied-$\varepsilon$ variant throughout (it captures the
\emph{effective} corruption, including input-side flips). On the noise axis
its advantage is real but bounded: best of all methods up to ${\sim}3\%$
($0.55$ vs $0.38$ at $2\%$), collapsing like the inverter beyond ${\sim}5\%$.
The registered calibration of its nominal $1\sigma$ predictive intervals
(the rev.-9 reporting item, completed here; the recomputation reproduces
every released grid median bit-exactly) tells the same story: empirical
coverage holds at or above the nominal ${\sim}68\%$ across the masking band
where the posterior is recommended ($0.86$ clean to $0.74$ at $50\%$
masking) but degrades along the noise axis ($0.58$ at $2\%$, $0.45$ at
$5\%$, $0.13$ at $20\%$)---beyond its range the posterior is not merely
inaccurate, its intervals are overconfident. That
noise-axis overconfidence is exactly the expected price of the entrywise
pseudo-likelihood: masking removes transitions cleanly (the independence
approximation stays benign), whereas bit flips corrupt inputs as well as
outputs, which the likelihood ignores.
We also quantified the known-radius assumption: selecting the radius by a
smallest-consistent rule costs ${\sim}0.12$ median \rtwo{} on clean diagrams
and degrades under noise (neighbourhood detection is itself a studied
identification subproblem~\cite{yangbillings2000}).

\emph{(ii) Learned rule-reader:} a small
($3\times10^4$-parameter) network---trained once, on one seed, per the
registered gating; seed variance at this capacity is untested---on
training-split rules to predict
table bits from degradation-augmented diagrams, feeding the exact simulator
either its thresholded (MAP) table or an average over tables sampled from its
per-bit probabilities. On held-out rules the reader recovers $0.73$ of table
bits and never a full table exactly, and the MAP variant fails everywhere
(median \rtwo{} $\le -2.2$): a single mostly-right table is a badly wrong
dynamical system. Sampling is what salvages the signal. The sampled variant is
the only one of the eight rev-9 estimators that does not collapse in the dead
zone (Fig.~\ref{fig:frontier}): from $7.5$ to $20\%$ noise it is the best of
those eight, holding median \rtwo{} in $[-0.04, 0.17]$---essentially flat, its bit
accuracy dropping only $0.73{\to}0.70$---while the inverter falls to $-5.6$
and the frozen CNN to $-0.93$; from $5\%$ it beats the inverter by the
registered rule at every cell.

\emph{(iii) Fairness controls} [registered numbers-free as rev.~10 before
measurement]. The comparison above hands the read-then-simulate family two estimators
adapted for degradation while the direct CNN stays clean-trained and
constrained, so we ran two symmetric controls, each a single seed on the
identical grid cells: the same CNN retrained with the reader's registered
degradation augmentation, and an unconstrained \texttt{resnet18} at the same
budget ($1.1\times10^{7}$ parameters, $354\times$ the constrained CNN;
clean-trained). On this fixed $80$-rule panel the result reverses one rev-9
conclusion and confirms the other. The noise ``dead zone'' was an
adaptation artifact, not an estimator-family boundary: the seed-0
degradation-trained CNN holds median \rtwo{} $0.51$--$0.62$ from $3\%$
to $15\%$ noise---where no previously tested estimator exceeded
$0.17$---meeting the registered extension rule (bootstrap CI excluding the
sampled reader's point value from below) at every noise cell up to $15\%$;
at $20\%$ that seed and the reader are comparable ($0.17$ vs $0.12$),
neither reliable. It pays a seed-dependent robustness tax on clean diagrams
($0.14$--$0.37$ on the grid's clean cells across the three seeds below)
and does not approach the posterior under masking ($0.48$ vs $0.85$ at
$40\%$) or extreme density ($0.20$ vs $0.77$). Meanwhile the third
registered hypothesis survives the controls: at every cell with per-bit
table recovery ${\ge}0.95$ the best read-then-simulate estimator remains
unbeaten---zero violations---and the unconstrained network is no better on
the grid's clean cells than the $31$k constrained one ($0.55$ vs $0.65$)
while degrading faster under noise ($-0.60$ at $20\%$); on the full
$160$-rule clean protocol it trades cone fill up for survival down at a
slightly lower median, and every mechanistic superiority verdict survives.
The matched-regime
negative result is not an artifact of the anti-shortcut constraint.

\emph{Panel replication} [registered, rev.~11]. Because the frontier rests
on a fixed $80$-rule subsample, we re-simulated the entire noise axis on the
\emph{complementary} $80$ held-out rules at identical budgets. The family
structure replicates: read-then-simulate owns the low-noise cells, the
degradation-trained network is strongest or statistically tied at every cell
from $3\%$ (point-strongest from $7.5\%$: $0.70/0.64/0.35$ at
$7.5/10/15\%$ against the pseudo-posterior's $0.61/0.37/{-0.19}$), and at
$20\%$ no estimator's CI clears zero. No cell winner is re-stated under the
registered rule. Two level shifts measure panel sensitivity in their own
right: the degradation-trained band reads $0.35$--$0.71$ on the
complement against $0.51$--$0.62$ on the canonical panel, and the
pseudo-posterior's collapse point moves---it holds $0.79/0.70/0.61$ at
$3/5/7.5\%$ on the complement where it had collapsed by $5\%$ on the
canonical panel---so the posterior-to-network crossover should be read as a
band, ${\sim}3$--$7.5\%$ noise, that depends on which rules are being
decoded, not as a sharp boundary.

\emph{Seed replication} [registered, rev.~11]. The degradation-trained
control was retrained under two further seeds, all else identical, against
a registered stability rule (each new seed's band-cell medians inside the
seed-0 CIs). The rule \emph{fails}: seed 2 passes every cell (band
$0.49$--$0.56$), but seed 1 trains to a much weaker model
($0.24$--$0.33$; its clean-cell and clean-panel scores drop in step, and
training loss converged identically, so this is run-to-run training
variance---validation medians swing by ${\sim}\pm0.2$ between late epochs
under augmentation, implicating final-epoch selection). Per the registered
rule we report the band as a per-seed range: median \rtwo{} $0.24$--$0.62$
across $3$--$15\%$ noise, two of three seeds in $0.49$--$0.62$. What is
seed-stable is the family-level ordering: at every cell from $5\%$ to
$20\%$ noise, \emph{every} seed's point median exceeds the best
read-then-simulate estimator's (all ${\le}0.17$ there); the extension rule
is met by seed 0 at $3$--$15\%$, seed 2 at $5$--$15\%$, seed 1 at $15\%$
only. At $20\%$, reliability is itself seed-dependent: seeds 1 and 2 clear
zero ($0.31$, $0.43$) where seed 0 and the complement-panel evaluation do
not. The corruption-adaptation conclusion stands at the family level; the
accuracy a practitioner should expect from a single training run at this
budget is uncertain by a factor of ${\sim}2$
(Table~\ref{tab:guidance}).

\emph{Verdicts.} The registered rev-9 hypothesis that the reader
extends beyond the posterior is confirmed on the noise axis only in a
weak sense---it moves the frontier without restoring identification---and is
refuted under masking, where the posterior holds to $50\%$ and the reader
never leaves zero; the rev-10 controls then supersede the reader in the
noise band entirely. Consistent with the third registered hypothesis,
re-tested with both controls included, at no
grid cell with high table recovery does any direct amortizer beat the best
read-then-simulate estimator. These curves are the paper's scope statement
in graphical form; Table~\ref{tab:guidance} states it as practitioner
guidance.

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{llll}
observation regime & use & median \rtwo{} evidence & \\
\colrule
matched (clean, full, known $r$) & exact inversion & simulation-limited & Tables~\ref{tab:eca}--\ref{tab:r2} \\
noise $\lesssim2\%$ & pseudo-posterior decoder & $0.55$ vs det $0.38$ at $2\%$ & Fig.~\ref{fig:frontier} \\
noise $3$--$7.5\%$ & either adapted family & crossover band; panel-dependent winner & Sec.~\ref{sec:frontier}(iii) \\
noise $7.5$--$15\%$ & degradation-trained direct CNN & $0.24$--$0.62$ across seeds (two of three $0.49$--$0.62$); replication panel $0.35$--$0.71$ & Sec.~\ref{sec:frontier}(iii) \\
noise $20\%$ & none reliably recommendable & CNN signal seed-dependent ($0.31$/$0.43$; seed-0 CI spans $0$) & Sec.~\ref{sec:frontier}(iii) \\
masking $\lesssim25\%$ & exact inversion & $0.89$ at $25\%$ & Fig.~\ref{fig:frontier} \\
masking $25$--$50\%$ & pseudo-posterior decoder & $0.85$ at $40\%$, $0.59$ at $50\%$ & Fig.~\ref{fig:frontier} \\
extreme IC density & pseudo-posterior decoder & $0.77$ vs det $0.08$ at density $0.1$ & Fig.~\ref{fig:frontier} \\
polarity freedom & any read-then-simulate & exactly invariant ($0.99$) & Fig.~\ref{fig:frontier} \\
unknown radius & smallest-consistent selection & ${\sim}0.12$ cost on clean diagrams & Sec.~\ref{sec:frontier} \\
\end{tabular}
\end{ruledtabular}
\caption{\emph{Exploratory} practitioner guidance, bounded to the evaluated
protocol: radius two, the fixed $80$-rule frontier panel, the exact
corruption distributions of Sec.~\ref{sec:identifiability}, and the
estimators actually tested (per-cell rule-bootstrap CIs accompany every
number in the released grid artifact). ``Use'' means the named estimator won
its cells under the registered rules; ``none reliably recommendable'' is a
claim about these estimators, not all estimators. Do not extrapolate beyond
the corruption model, architecture, and training distribution
evaluated---the frontier's own lesson is that adaptation to the observation
model decides these races.}
\label{tab:guidance}
\end{table*}

\subsection{Cost: accuracy versus compute}
\label{sec:compute}

This subsection and the next are \emph{secondary} analyses; every claim of
the paper rests on Secs.~\ref{sec:results-mech}--\ref{sec:frontier}. The
mechanistic estimator pays per query (rule inference plus a fresh
simulation); the network pays once. In a deployment vignette on one
machine---both methods timed on the \emph{same} single CPU core,
warm-started, per diagram [exploratory, rev.~8]---the mechanistic estimator
costs $283$\,ms per diagram at the production budget ($256$ pairs;
embarrassingly parallel over diagrams), against $1.0$\,ms for a CNN forward
pass on that thread ($2.0$\,ms batched at $256$; $2.2$\,ms on the Metal GPU,
where dispatch overhead dominates a $31$k-parameter model) and $1.4$\,ms for
the five statistics; one-time costs ($\approx22$\,min per CNN seed versus
$15$\,s for the boosted baseline) give a break-even of $\approx4700$
queries. The accuracy-versus-budget curve, however, is one-sided: even at a
$16$-pair budget ($17$\,ms per diagram) the mechanistic median \rtwo{} is
$0.924$, above both direct estimators at any cost, and $32$ pairs ($32$\,ms)
give $0.966$. Under the matched observation model the mechanistic estimator
is the most accurate at every simulation budget we measured, so the
network's case must rest on degraded-observation regimes
(Fig.~\ref{fig:ident}) or extreme-throughput screening, not on accuracy.

\subsection{Descriptive by-products, with independent ground-truth evaluation}
\label{sec:constructive}

\emph{Phenotype maps for non-uniform CA.} Sliding the pretrained
five-statistic regressor over a composed (two-rule) diagram yields an
interpretable phenotype map with no additional training at deployment
(trained once; not ``training-free''). Spatially resolved phenotype fields
are an established instrument---the local statistical-complexity filters of
computational mechanics~\cite{shalizi2006}---ours predicting a
perturbation-defined quantity instead of structural complexity. Scored
against an independent location-resolved ground truth (the damage response
measured by local perturbation \emph{in} the composed system; window width
chosen on a validation split of rule pairs; the composed system as the unit
of resampling), the learned map's advantage is \emph{inconclusive} under the
registered framework: mean Spearman $0.67$ vs $0.61$, $\Delta=+0.047$,
$95\%$ CI $[-0.002,+0.102]$, $90\%$ CI $[+0.005,+0.092]$---not superiority
(below the $0.10$ margin), not equivalence (the $90\%$ CI exceeds
$\pm0.05$). The honest summary is a small, statistically unresolved edge for
the learned map at $15$-column resolution.

\emph{A damage-signature landscape of the radius-two space.} The same
statistics over $3000$ uniformly sampled canonical rules give
Fig.~\ref{fig:landscape}: $7.8\%$ (Wilson $95\%$ CI $[6.9,8.8]\%$) of rules
satisfy the registered damage-signature criterion (persistent,
sub-ballistic, sparse-cone damage), versus $3/88$ elementary orbits---a
radius-two analogue of Li and Packard's map of the elementary rule
space~\cite{lipackard1990}, drawn in damage coordinates. This is the
prevalence of a \emph{finite-horizon criterion} under the stated sampling
distribution, not a validated glider census. On ECA, where literature ground
truth exists, the signature selects $\{54,106,110\}$: both canonical
class-IV rules (sensitivity $1.0$) with one false positive, rule $106$,
which the classical schemes list as chaotic~\cite{wolfram1984,lipackard1990}
but whose damage signature sits between the chaotic and complex clusters in
our measurements (precision $2/3$, specificity $0.99$, balanced accuracy
$0.99$). An \emph{independent} localized-seed glider detector is far weaker
as an instrument (vs.\ the particle censuses of the
literature~\cite{boccara1991,wuensche1999}: sensitivity $0.5$, missing rule
$54$, whose ether front advances at light speed; precision $0.17$), and on
radius two it agrees with the signature barely better than chance (balanced
accuracy $0.39$, precision $0.05$, $231$ applicable rules).
Single-observable glider detection is unreliable in the large space, which
is why we do not promote the $7.8\%$ region to ``complex'' or
``glider-supporting'': that would require long-horizon curated validation we
have not done.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/landscape.pdf}
\caption{The radius-two \emph{damage-signature landscape} ($n=3000$
uniformly sampled canonical rules; the $800$-rule estimator panel is a
subset). Red: rules satisfying the registered criterion---persistent,
sub-ballistic damage in a sparse cone; they form a band between the ordered
arm and the chaotic bulk, and the embedded class-IV ECAs $54$ and $110$
(stars) land in it. The band is \emph{not} an independently validated glider
region; prevalence and the ECA validation are in
Sec.~\ref{sec:constructive}.}
\label{fig:landscape}
\end{figure}

\section{Discussion}
\label{sec:discussion}

Under a fully observed, noiseless, deterministic, synchronous, binary
observation model with known radius, a single space--time diagram identified
the local rule exactly for every elementary and $97.5\%$ of radius-two
held-out rules, and reconstructing the generator then re-running the
stipulated experiment predicts finite-horizon damage response at the
accuracy of re-measuring it. That is the central finding, and we believe it
generalizes: \emph{a nominally single-observation amortization benchmark can
be solved by system identification whenever the observation encodes the
generator}. Benchmark designers who intend to test representation learning
must break that encoding (noise, partial observation, unknown model class)
or accept that they are benchmarking identification.

The lesson connects literatures that do not currently cite one another. In
continuous-state settings the identification route is equation discovery:
sparse and symbolic regression recover governing equations from clean,
densely sampled data~\cite{brunton2016,schmidtlipson2009}, and once the
generator is recovered, every simulation-defined functional of it---Lyapunov
exponents included---follows by re-running; a damage-response benchmark posed
on such systems would face the same baseline. Simulation-based inference
names the trade we quantify---amortize training cost against per-query
simulation~\cite{cranmer2020}---and the identifiability frontier is a worked
example of choosing between an amortized predictor and per-observation
inference on observation quality. At field scale, McGreivy and Hakim document
that most machine-learning-for-PDE papers claiming to beat standard numerics
compared against weak baselines~\cite{mcgreivy2024}; this benchmark is the
controlled, closed-world version of that critique, with the strong baseline
constructed explicitly and calibrated against replicate reliability.

The neural results are deliberately scoped. For \emph{this} constrained
architecture at \emph{this} training budget, direct regression neither reaches
the reliability benchmark nor dominates five cheap statistics, and the two
trade places by target; the rev-10 unconstrained control shows this is not
the constraint's doing: an architecture $354\times$ larger at the same
budget changes no verdict (Sec.~\ref{sec:frontier}). The network's one demonstrated
edge---incremental survival information, $+0.32$ over the five-statistic
base and $+0.08$ over the boosted baseline of record---is real, is exactly
where texture is least informative, and survives cross-fitting. We do not
conclude that deep
networks cannot close the gap. The probes show rule information is accessible
to the network; whether exploiting it is an architecture, objective, or
optimization question is open. The rev-9 rule reader and the rev-10
degradation-trained control sharpen that question: in the noise band beyond
${\sim}3$--$7.5\%$ the strongest estimator is the direct network trained for
the corruption, the sampled reader merely holds \rtwo{}${\approx}0$, and
neither approaches simulation-limited accuracy; under masking and extreme
densities the Bayesian posterior, not either learned route, extends the
mechanistic route. Whether a larger reader, a differentiable simulator in
the loop, joint reader--simulator training, or corruption-trained direct
networks at scale close that gap is now a concrete, measurable open problem,
next to rule-scrubbing and rule-provision interventions to test causality.

How far does the frontier travel beyond binary deterministic CA? Each axis
has a named analogue, and we mark what is measured here versus conjectured.
Observation noise maps to
measurement noise in equation discovery, where recovery is likewise
noise-brittle~\cite{brunton2016}: the collapse is measured here; the repair
ordering (exact, then Bayesian, then corruption-trained learned estimators)
is established for CA only. Masking maps to missing sensors and partial observation:
measured here, transfer conjectured. Initial-condition density maps to
insufficient excitation---the persistency-of-excitation condition of
classical system identification, of which our under-exercised rule tables
are the finite-alphabet form: measured here. Label flips map to symmetry
freedoms, under which equivariant estimators are invariant by construction:
measured. Known radius maps to known model order or library: its relaxation
is measured here at a ${\sim}0.12$ median-\rtwo{} cost. A stochastic
generator, by contrast, changes the problem in kind---single-diagram
identification no longer reduces to transition tabulation, and the replicate
benchmark becomes the operative ceiling---so nothing here licenses
conclusions for that case.

Every claim is bounded by the protocol tuple (Bernoulli$(1/2)$ ICs, ring
$127$, radius-aware horizon): the statistics are finite-horizon and
measure-dependent by construction~\cite{gilman1987,culikyu1988}, and
Fig.~\ref{fig:ident}(d) exhibits that dependence directly. Within that scope
the practical guidance is plain and carries its price tag
(Sec.~\ref{sec:compute}): if the observation model is matched, read the rule
and simulate; under moderate corruption, read it
\emph{probabilistically}---the pseudo-posterior decoder holds to
${\sim}3$--$7.5\%$ noise (panel-dependent), $50\%$ masking, and extreme
densities at no training cost, with calibrated intervals exactly where it is
recommended; beyond that, train for the corruption you expect---the
degradation-augmented direct network holds median \rtwo{} $0.24$--$0.62$
across $3$--$15\%$ noise depending on the training seed (two of three:
$0.49$--$0.62$; expect ${\sim}2\times$ run-to-run variance at this budget)
at a $0.14$--$0.37$ clean-diagram cost, and at $20\%$ noise or past $50\%$
masking no tested estimator can be reliably recommended
(Table~\ref{tab:guidance}). For the broader program of behaviour-first
(phenotype) analysis of
CA~\cite{wolfram2002,lipackard1990,israeli2006}, the damage-signature
landscape and the composed-system maps stand as usable descriptive
artefacts, described at the strength their independent ground-truth
evaluation supports and no stronger~\cite{kleinberg2002,mitchell1993}.

\begin{acknowledgments}
We thank the anonymous referees, whose three reports directly shaped
revisions~7--11 of the registered evaluation criteria and the controls
reported here.
% TODO(user): add funding sources before submission.
\end{acknowledgments}

\section*{Author declarations}

\emph{Conflict of interest.} The authors have no conflicts to disclose.

\emph{Author contributions.} \textbf{Michiel Rollier}: conceptualization,
methodology, software, investigation, writing---original draft.
\textbf{Jan M.~Baetens}: conceptualization, methodology, supervision,
writing---review \& editing.
% TODO(user): confirm CRediT roles before submission.

\section*{Data availability}

The complete pipeline, the pre-registered decision rules with their full
git revision history (revisions~2--11), the measured record, the run
artifacts backing every number in this article, and a programmatic audit
that re-derives the quoted values from those artifacts are released with
the article as a timestamped repository archive; the archive records the
exact commit hash of the version that produced this manuscript. Environment:
Python~3.11 on Apple silicon (fp32), PyTorch, scikit-learn pinned at 1.9.0;
\texttt{pip install -e .} recreates it. Reproduction entry points:
\texttt{scripts/audit\_manuscript\_numbers.py} re-derives every quoted
number from the released artifacts (exit code $0$ on agreement), and
\texttt{manuscript/make\_figures.py} regenerates every figure.
% TODO(user): mint the archive DOI on acceptance and insert it here.

\appendix

\section{Reproducibility and registration status}
\label{app:repro}

All experiments run on one Apple-silicon machine (Metal/CPU, fp32); the
pipeline is first-party except PyTorch and scikit-learn. Targets, splits and
panels are keyed on fixed seeds; the mechanistic estimator and reliability
replicates use random streams provably independent of the target cache (prime
seed offsets on per-rule \texttt{SeedSequence}s), so agreement between them is
never contamination. All decision rules, margins and panels were committed to
a timestamped private repository before the corresponding measurements; the
full history is released with the article. Because an in-review repository is
not independently auditable, we label every analysis here rather than lean on
the word ``pre-registered'': \emph{registered} (rule committed numbers-free
before measurement): the damage-signature criterion, holdout split, paired
inference and TOST margins, stacking margin, reliability design, mechanistic
estimator and completion policy, probe design, detector-validation design,
map ground-truth design, and---for the frontier of
Sec.~\ref{sec:frontier}---the grid design, estimator definitions, verdict
rules, the three hypotheses below (rev.~9), and the two fairness controls
with their verdict rules (rev.~10); \emph{post-hoc confirmatory}
(analysis added at revision, on data or rules not used to tune it): the
replicate-agreement correction, the large-simulation reference,
confusion-matrix reporting, TOST margin sensitivity, the Bayesian
rule-posterior evaluated on the pre-existing noise/mask/density axes,
the headline-base stacking variant (rev.~10), and the seven rev.-11
review-response analyses (reference rescoring, paired equivalence,
enriched-panel decomposition, per-diagram audit, seed replication,
deployment-style stack, complement-panel noise-axis replication---each with
its decision rule committed numbers-free before measurement);
\emph{exploratory}: the identifiability phase diagram, the accuracy--compute
curve, the learned rule-reader, and the label-noise and unknown-radius axes.

The three frontier hypotheses (rev.~9) were registered verbatim before
measurement; their earned verdicts---\emph{(i)} the posterior dominates the
deterministic inverter at intermediate noise: \emph{confirmed}; \emph{(ii)}
the reader extends further than the posterior: \emph{split}, weakly
confirmed on noise and refuted under masking; \emph{(iii)} no direct
amortizer beats the best read-then-simulate estimator at high table
recovery: \emph{confirmed, zero violations}---are reported in
Sec.~\ref{sec:frontier}, together with the one reporting item outstanding
from rev.~9 (the calibration of the posterior's nominal $1\sigma$
predictive intervals), completed in this revision.

One deviation from the original registration is
disclosed: an early implementation seeded target streams by panel position
rather than rule identity (cross-panel drift up to $0.05$--$0.08$ on
borderline-survival rules); it was replaced by identity keying and the drift
verified to vanish.

\section{Methods supplement}
\label{app:methods}

\emph{Data.} ECA: all $88$ orbit representatives, $256$ diagrams per rule;
radius two: $800$ canonical rules ($857$ drawn, deduplicated by the order-four
reflect/complement orbit), $64$ diagrams per rule; all diagrams
$127\times127$, Bernoulli$(1/2)$ ICs, no transient discarded. \emph{Sampling
measure:} radius-two rules are drawn uniformly over the $2^{32}$ raw
encodings, folded to canonical orbit representatives, and deduplicated;
orbits therefore enter with probability proportional to orbit size (the
natural random-rule measure), \emph{not} uniformly over orbits, and every
prevalence in this paper (including Fig.~\ref{fig:landscape}'s $7.8\%$) is
under this measure. Observed
diagrams and target-estimation streams are disjoint by construction (dataset
seed vs.\ target seed), and no random numbers are shared across methods.
Splits: leave-rules-out, holdout fraction $0.2$, stratified by Wolfram label
where available, seed $0$; radius two force-holds the $57$ signature-complex
panel rules (so the complex subset is never trained on); ECA force-holds rule
$110$ and force-trains rule $54$. Hyperparameters were tuned only on training
rules.

\emph{Evaluation protocol (the unit behind every number).} (1)~A test
instance is a (rule, diagram) pair. (2)~The direct estimators (ridge,
boosted, CNN) predict per diagram and their predictions are \emph{averaged
within rule} before scoring; the mechanistic and probabilistic decoders
predict from the rule's \emph{first} diagram only, with cross-diagram
stability audited separately (Sec.~\ref{sec:estimators}); the asymmetry
favours the direct estimators. (3)~Every reported \rtwo{} is computed at the
rule level: unfitted $1-\mathrm{SS}_{\mathrm{res}}/\mathrm{SS}_{\mathrm{tot}}$
per target over held-out rules, with the held-out panel mean in the
denominator and no refitting; a constant target column yields NaN and is
excluded from medians; negative values are reported as computed (frontier
bootstrap draws alone are clipped to $[-1,1]$ for the median CI). (4)~The
four-target ``median'' is the median of the four per-target values.
(5)~Bootstrap resampling always draws \emph{rules} with replacement ($10^4$
draws); diagrams are already collapsed into their rule's prediction, so the
diagram-within-rule structure is preserved by construction. (6)~Coverage and
exact-reconstruction rates in the main text are per rule (first diagram);
the per-diagram audit [rev.~11] reports the other unit.

\emph{Targets.} With flip position $x_0$ uniform on the ring, horizon
$T=\lfloor w/(2r)\rfloor-1$ (capped so a single flip's light cone cannot wrap
the periodic boundary), and $d\in\{0,1\}^w$ the cellwise disagreement of the
twin runs at time $T$:
survival $=\Pr[\exists i: d_i=1]$; fraction $=\mathbb{E}[\bar d\mid\text{surv}]$;
rate $=\mathbb{E}[\mathrm{ext}(d)/(2rT)\mid\text{surv}]$; fill
$=\mathbb{E}[\sum_i d_i/\mathrm{ext}(d)\mid\text{surv}]$. The extent is
computed in flip-centred ring coordinates
$p_i=(i-x_0+\lfloor w/2\rfloor)\bmod w$ over damaged cells as
$\mathrm{ext}(d)=\max_i p_i-\min_i p_i+1$; a single damaged cell has
$\mathrm{ext}=1$ and no wrap can occur under the horizon cap. Conditioning on
survival makes $d$ nonempty, so both conditional denominators are strictly
positive. Conditional targets
are $0$ when no pair survives (itself the ordered-rule signature); surviving
trial counts therefore vary by rule, one reason survival-adjacent targets are
heteroscedastic and all reliability intervals are bootstrapped.

\emph{Network.} Input $1\times127\times127$; four stages of [Conv $2\times2$,
stride 1, no padding $\to$ BatchNorm $\to$ ReLU $\to$ MaxPool $2\times2$] with
channels $16/32/64/64$; $1\times1$ convolution to a $64$-d bottleneck with
BatchNorm and no final nonlinearity; global average pool; linear head to $4$
targets ($31{,}524$ parameters; receptive field $31\times31$ per bottleneck
position before pooling). AdamW, lr $10^{-3}$, weight decay $10^{-6}$, batch
$128$, $60$ epochs, no early stopping; targets standardized on training rules.
Augmentations: cyclic shift, horizontal flip, polarity inversion (the exact
orbit symmetries). Training cost $\approx22$\,min (radius two) and
$\approx10$\,min (ECA) per seed on the reference machine.

\emph{Baselines.} Ridge ($\alpha=1$) and scikit-learn
\texttt{GradientBoostingRegressor} (defaults, one per target) on the five
features standardized on training rules; the boosted model is stronger on both
spaces and is the baseline of record. Split sensitivity of the baseline:
leave-one-orbit-out over the $88$ ECA orbits gives per-target \rtwo{} $0.73$
(survival), $0.85$ (fraction), $0.95$ (rate), $0.93$ (fill), consistent with
the registered-split values; over $20$ repeated radius-two outer splits the
per-target median (5th--95th percentile) is $0.76$ $(0.56$--$0.84)$ for
survival, $0.92$ $(0.90$--$0.93)$ for fraction, $0.90$ $(0.87$--$0.91)$ for
rate and $0.68$ $(0.55$--$0.75)$ for cone fill---the noisy targets carry
substantial split variance.

\emph{Verdicts at other margins.} At $\delta=0.02$ the single equivalence
verdict (ECA spreading rate, CNN vs.\ baseline) reverts to inconclusive; at
$\delta=0.10$ radius-two fraction and rate additionally become equivalent; no
superiority verdict changes at any margin, so the ``no stable ordering''
conclusion is margin-insensitive.

\emph{Panels.} The $800$-rule estimator panel is a subset of the $3000$-rule
landscape sample (same sampling stream); landscape prevalence is therefore
computed on the superset and estimator results never touch the extra $2200$
rules. The reliability panel subsamples $250$ of the $800$ (fixed seed).

\section{Decoder algorithms, clean and degraded}
\label{app:algorithms}

Every decoder in this paper reduces to the same sufficient statistics: for
neighbourhood index $k\in\{0,\dots,2^{2r+1}-1\}$, the counts
$(\mathrm{ones}_k,\mathrm{total}_k)$ of observed transitions.

\emph{A1: tabulation.} For every $t<T$ and cell $i$ (periodic in space, the
$t{=}0$ row included as an input row), the index is
$k=\sum_{o=-r}^{r} s_{t,i+o}\,2^{\,r-o}$ (leftmost neighbour most
significant) and the recorded output is $s_{t+1,i}$. The deterministic table
is the per-entry \emph{majority}, $\mathrm{bit}_k =
\mathbb{1}[2\,\mathrm{ones}_k > \mathrm{total}_k]$ (ties to $0$); entries
with $\mathrm{total}_k=0$ are unobserved and completed by the registered
default-$0$ policy; an inconsistency flag records whether any observed entry
was non-unanimous (impossible on clean deterministic data).

\emph{A2: degradations.} Bit-flip noise flips every cell of the diagram
independently with probability $p$, including the $t{=}0$ row, before
tabulation. Masking marks cells unobserved; a transition at $(t,i)$ enters
the counts only if its output cell $(t{+}1,i)$ \emph{and} every input cell
within radius $r$ of $(t,i)$ are observed. At each grid cell of
Sec.~\ref{sec:identifiability} the same corrupted diagram is given to every
estimator.

\emph{A3: pseudo-posterior decoding.} Given a flip rate $\varepsilon$
(clamped away from $\{0,\tfrac12\}$) and a per-entry Bernoulli prior $p_0$
(the observed marginal one-frequency; $\tfrac12$ when nothing is observed),
the per-entry posterior log-odds are
\[
\ell_k=\log\frac{p_0}{1-p_0}
+\bigl(2\,\mathrm{ones}_k-\mathrm{total}_k\bigr)\,
\log\frac{1-\varepsilon}{\varepsilon},
\qquad P(\mathrm{bit}_k{=}1)=\sigma(\ell_k),
\]
i.e.\ output-cell flips only, entries treated independently. When
$\varepsilon$ is not supplied it is estimated by observation
self-consistency: the disagreement fraction
$D=\sum_k\min(\mathrm{ones}_k,\mathrm{total}_k-\mathrm{ones}_k)/\sum_k
\mathrm{total}_k$ is matched to the nearest value of the fixed grid
$\{0,0.005,0.01,0.02,0.03,0.05,0.075,0.10,0.15,0.20\}$ [registered, rev.~9].
The estimate is the average of the simulated damage response of $8$ tables
sampled from the independent per-entry posteriors; their spread is the
predictive interval whose calibration Sec.~\ref{sec:frontier} reports.

\emph{A4: radius selection.} Candidates $r\in\{1,2,3\}$: compute $D$ at each
candidate and select the \emph{smallest} whose $D$ is within $0.005$ of the
minimum; a selected $r{<}2$ table is embedded exactly, a selected $r{=}3$
table is projected to radius $2$ by count-weighted majority over its middle
five index bits.

\emph{Worked example} (radius $1$, $\varepsilon=0.05$, $p_0=\tfrac12$). A
neighbourhood observed $30$ times mapping $28{\to}1$, $2{\to}0$: A1 restores
$\mathrm{bit}=1$ by majority and sets the inconsistency flag; A3 gives
$\ell=(56-30)\log(0.95/0.05)\approx 77$, certainty. A sparsely exercised
entry seen $3$ times mapping $2{\to}1$, $1{\to}0$ gets $\ell=\log
19\approx2.9$, $P(\mathrm{bit}{=}1)\approx0.95$: sampled tables disagree
exactly on the entries the diagram cannot pin down, which is what makes the
sampled-table average and its interval informative.

\section{Prior work under its assumptions}
\label{app:priorwork}

Table~\ref{tab:priorwork} tabulates the identification literature the
Introduction builds on, by observation regime and method, so the boundary of
our novelty claim is auditable: single-diagram CA identification is
established prior art; what no prior work measures is its \emph{consequence
for a behaviour-amortization benchmark}---the calibrated benchmark gap,
the reliability-aware ceiling, and the identifiability frontier.

\begin{table*}[t]
\centering
\begin{ruledtabular}
\begin{tabular}{lllll}
work & CA / data regime & neighbourhood & inference method & task \\
\colrule
Richards \emph{et al.}~\cite{richards1990} & experimental (dendrites), noisy & unknown & genetic search over rule sets & extract CA dynamics from data \\
Adamatzky~\cite{adamatzky1994} & deterministic \& probabilistic & known/unknown & transition tabulation, theory & identification of CA (monograph) \\
Yang \& Billings~\cite{yangbillings2000} & noisy observations & unknown & orthogonal least squares & rule + neighbourhood recovery \\
Sun \emph{et al.}~\cite{sunrosinmartin2011} & deterministic, 1D/2D & unknown & incremental identification & fast rule + neighbourhood selection \\
Elser~\cite{elser2021} & nonconsecutive snapshots & known & constraint projections & rule + hidden-state recovery \\
Gilpin~\cite{gilpin2019} & clean diagrams & known & CNN $\equiv$ CA construction & representation equivalence \\
Mordvintsev \emph{et al.}~\cite{mordvintsev2020} & target behaviour, differentiable & known & gradient descent on rule & learn rule to match behaviour \\
Rollier \emph{et al.}~\cite{rollier2024cnn} & clean single diagrams & known & CNN classifier & ECA rule identity ($99.9\%$) \\
McGreivy \& Hakim~\cite{mcgreivy2024} & ML-for-PDE literature & --- & meta-analysis & weak-baseline prevalence \\
this work & matched \& degraded binary CA & known + selected & tabulation / pseudo-posterior / reader & benchmark consequence + frontier \\
\end{tabular}
\end{ruledtabular}
\caption{Prior work on CA identification and the adjacent benchmark
literature, by assumptions. None of the identification rows measures the
consequence of identifiability for a perturbation-defined behaviour
benchmark; none of the benchmark rows constructs the identification
baseline.}
\label{tab:priorwork}
\end{table*}

\emph{Detector.} The localized-seed detector evolves a single seeded cell on a
quiescent background (ring $255$ confirmation) and flags persistent, localized,
sub-ballistic, non-space-filling structures (thresholds fixed in code before
radius-two application: alive fraction $\ge0.5$, growth rate $0.05$--$0.9$,
fill $\le0.75$). Its failure modes on ECA are documented in
Sec.~\ref{sec:constructive}; a multi-observable detector (periodicity,
collisions, comoving localization) would in effect rebuild the structure
filters of computational mechanics~\cite{shalizi2006} and was not built
here.

\emph{Composed-system maps.} Half/half mosaics over the $16$-rule panel listed
in the released configuration; pairs with spreading-rate gap $\ge0.25$; $8$
ICs per pair; local ground truth by single-cell perturbation at the map's $15$
column centres with a $24$-step local horizon and $64$ pairs per location,
independent streams. Window width selected once on the even-indexed pairs;
odd-indexed pairs are the test set; the per-pair best window appears only as a
labelled oracle sensitivity ($0.638$, between the validation-selected $0.605$
and the CNN's $0.673$).

\bibliographystyle{apsrev4-2}
\bibliography{refs}

\end{document}
